# 修复 macOS 画中画 (PiP) 模式无法调整播放进度

## 项目信息
- 根目录：`/home/tar/workspace/bili-fm`
- 技术栈：Tauri v2 + React + TypeScript
- 目标文件：`src/components/playerVideo.tsx`（237行）

## 问题
macOS WKWebView 下，`<video controls>` 进入画中画（Picture-in-Picture）模式后，进度条无法拖动。
根因：macOS WebKit 的 PiP 浮窗是只读预览，不渲染用户控件，进度调整被丢弃。

## 约束
1. **只改 `src/components/playerVideo.tsx`** 及相关 CSS
2. 不要改 `src/components/player.tsx`（音频播放器）
3. 不要改播放进度上报逻辑（reportCloudProgress）
4. 不要改 mini-mode（窗口化迷你模式）
5. 不要引入新 npm 依赖
6. 不要 commit/push
7. 完成后 `npm run build` 必须通过

## 方案：自定义控件 + PiP API

### 改动点

1. **移除 `<video controls>`**，改用 React 自定义控件条

2. **添加状态**：
   - `isInPiP: boolean` — 是否在画中画模式
   - `isPlaying: boolean` — 播放状态（从 video 事件同步）
   - `currentTime: number` — 当前播放时间
   - `duration: number` — 总时长

3. **监听 PiP 事件**：
   - `enterpictureinpicture` → `setIsInPiP(true)`
   - `leavepictureinpicture` → `setIsInPiP(false)`

4. **自定义控件条 JSX**（在 video 下方或上层）：
   - 进度条：`<input type="range">`，拖动时 `video.currentTime = newTime`
   - 播放/暂停按钮
   - 当前时间 / 总时长显示
   - 画中画按钮：`video.requestPictureInPicture()`
   - 退出画中画按钮（PiP 模式下显示）：`document.exitPictureInPicture()`

5. **画中画按钮逻辑**：
   - 检测 `document.pictureInPictureEnabled`，不支持则隐藏按钮
   - 调用 `video.requestPictureInPicture()`，失败时 catch

6. **PiP 模式下隐藏自定义控件条**（浮窗是只读的，显示控件无意义）

7. **关闭 playerVideo 时清理**：
   - 如果 `isInPiP`，先 `await document.exitPictureInPicture()` 再 `setIsplay(false)`

8. **切换 src 时清理**：
   - 如果 `isInPiP`，先 `await document.exitPictureInPicture()`

### CSS 风格
- 参考 `src/components/player.tsx` 中 `.player-timeline` 的进度条样式
- 控件条半透明背景，底部水平排列
- 图标用 `@icon-park/react`（项目已用）

## 验证
1. `npm run build` 通过（TypeScript 检查）
2. `git diff --stat` 只改了 `src/components/playerVideo.tsx` 和可能的 CSS 文件
