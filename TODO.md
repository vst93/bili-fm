# TODO / 待修复

> 测试环境：Windows（预览版 2.0.9），按优先级记录，修复后请勾选并注明提交。

## [ ] Bug 1：播放页二次打开显示异常

**现象**：点击视频进入播放页后关闭，再对**同一个视频**再次播放时，播放页弹出/显示异常——页面组件渲染不出来，连关闭按钮都不可见，整体不可用。

**复现步骤**：
1. 列表中点击视频 A，进入播放页
2. 关闭播放页
3. 再次点击同一个视频 A
4. 播放页弹出异常，组件缺失、无法关闭

**初步定位方向**：播放页组件关闭时可能未正确销毁/重置状态（疑似与 DialogProvider 的关闭流程、onClose 时机有关，2.0.9 刚改过 `src/components/dialog/DialogProvider.tsx` 的 onClose 调用与 backdrop 关闭逻辑）。

**期望**：同一视频可反复正常打开/关闭播放页。

---

## [ ] Bug 2：播放列表头部不应显示自定义菜单栏按钮

**现象**：播放列表展示时，头部同时渲染了 Windows 自定义标题栏的按钮组——最小化、关闭、设置按钮全部可见。

**期望**：
- 头部只保留**顶栏拖动功能**（保留那层悬浮阴影的拖动逻辑）
- 最小化、关闭按钮可以保留（当前表现正常）
- **设置按钮必须隐藏**（Windows 自定义菜单和 Linux 自定义菜单下都隐藏）——目前点击后也无法正常显示，属于无功能按钮

**涉及方向**：Windows / Linux 自定义标题栏组件（设置按钮按平台隐藏），播放列表头部渲染逻辑。
