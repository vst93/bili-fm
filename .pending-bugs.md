# bili-fm 待办：内存优化轮（2026-09-11，数字已校准）

## ⚠️ 2026-09-11 修正：此前从截图 OCR 误读 10 倍（小数点被吞）
- 真实值（4x 放大复核）：WebView2 组(7)合计 221.8MB = GPU 94.0 + 渲染 76.4 + 其余 ~48 + 隐藏第7行 ~3
- 结论：**内存水平健康，无优化必要**；GPU cap flag 在此用量下无效(纯天花板)，光场简化开关真实收益 ~10-30MB
- 已推送 dc94b30（devtools 摘除仍值得/GPU flag 无害无效/光场开关保留）+ 032fcb6（Cargo.lock）
- 2026-09-11 晚：7fac843 光场开关重构为「高级质感」(key premiumTexture,默认开) + 修开关打架(ambient-off 加 .ambient-off.texture-basic 组合选择器保证优先) + 旧偏好迁移；双远端 tip 一致
- 打磨轮1 543e014: ErrorBoundary/抽屉骨架/搜索loading/历史重试/412友好提示(5项);轮2 1ff81d1: 本地断点续播(localStorage LRU-50,max(云,本地)合并)+抽屉会话缓存(模块级LRU-3,TTL15min,DOM照卸)。均双远端一致
- 打磨轮3-7(用户否决关窗选项/主题跟随两项): 247fa89 倍速音量记忆+单曲循环三态+urlJump toast; 6d8aa1b 进度条缓冲指示+hover时间气泡; 849b156 全局媒体键(global-shortcut插件重新引入+capabilities三权限); e4edcd2 歌单导入导出(dialog插件,blocking调用于spawn_blocking——pi初版在async command裸用blocking_save_file会被钉死worker,Hermes审查抓出); 338baa9 SponsorBlock恰饭跳过(开关默认关在EQ旁,每段仅自然跨入跳一次,fetch 3s超时静默降级,tests 18pass)。全部双远端tip一致
- 迁移修 c974404: 导入导出入口从孤儿页about.tsx迁到歌单抽屉头部(审查漏查可达性的教训); 轮8 3c09631: 音量按钮纯开关化+面板内独立静音控件(原onClick隐式静音bug)、SponsorBlock改Ad图标、mini隐藏EQ/倍速/跳过(功能仍生效)、进度条浅红广告段标记层
- a1946d9: 用户决定删除歌单导入导出功能(含dialog插件整体移除,-344行);轮9 c40102b: 修进度条被旧网格列压短——主窗sponsor列116px是文字按钮遗产改40px,mini隐藏3开关后3列死轨道92px仍占位改5列模板
- 轮10 fc5338b: mini transport列实测63px(32play+3gap+28next,上轮46px溢出——Hermes两次grid假设均错,教训:布局修复必须按DOM实际按钮集合推导)+时间列56px; 8类卡片meta行统一(播放量x.x万+相对时间,container query窄卡先藏时间后藏播放量); 合作视频badge(staff非空判据,仅详情页,列表接口无可靠字段); 三键总量数值(VideoStat同响应解析零新增请求,1.1w/23w格式)
- 轮12 281a3f4(第二次回报,pi推翻轮11): mini网格5列但6个item(.player-speed包裹div只藏内部按钮留空item折隐式行顶高4px)→藏包裹+网格1:1;跳过按钮轮8起mini display:none不参与hit-testing(用户在mini操作);教训:报障先确认用户在哪个窗口操作
- 轮15 ffc5e45(用户授权根治): mini-mode类3个写入口时序交错可脱钩→主窗残留mini-mode则sponsor按钮display:none命中空气="开关没反应"真候选。修:state唯一事实源,effect唯一writer;toggleSponsorSkip持久化失败toast可见;+6回归测试28pass
- 轮17 eec3f8a(pi推倒重来+Playwright真实Chromium实证): AD按钮根因=深色时段CSS级联丢失——html:is() #player .player-button(1,2,1)盖过基础激活规则(1,2,0)→晚间/夜间点击后data-active已置位但按钮仍灰(EQ有深色专属激活规则所以变蓝=用户看到的精确差异)。修:深色块+prefers-contrast块补sponsor激活规则+契约测试锁定(30pass);前轮探针未触发说明一切正常恰与"只有样式丢"自洽;教训:toggle类控件查激活样式要先按时段/对比度/平台分支过一遍,不只看基态
- 轮19+20 74ad693: ①SponsorBlock二轮溯源=2.0.15确实含CSP修复,真凶=开关默认关且设置面板从无入口+WebView2 fetch路径不可控→查询下沉Rust reqwest(get_sponsor_segments,5s超时失败返空)+AD按钮状态点(绿N段/黄请求中/红失败/灰未启用)待用户截图定位 ②卡片二轮=时长挪封面角标,meta=作者·播放·时间,优先级时长垫底,series/pageList补字段 ③合作标签删主页重复胶囊,列表接口确认无字段,CardMeta留badge槽 ④内存=r20 drawerCache.ts抽出:单条目裁120+全局预算300+弹幕80/评论60,WebView2 MemoryUsageTargetLevel评估不做(需新依赖过度工程);当前origin tip=74ad693
- 轮13 e17bc51: mini入口恢复隐藏(回5列1:1);跳过功能根因=sponsorBlock会话缓存污染——abort请求catch返回[]被cache.set钉死空+inflight共享doomed promise→segments永远空。修:仅真实服务端结论缓存/abort不写可重试/inflight按signal隔离;+4测试22pass;教训:catch降级路径勿与成功路径共用缓存写入;当前双远端tip=ffc5e45
- P1/P2 清单（backdrop 分级/图片缓存/回帖虚拟化）判定为不必要，不做
- 轮21: ①时长角标补全列表:collectList/recommendList 把 duration 从 meta 行搬到封面(c-cover-duration),searchList 新增 length 字段(Rust parse_search_results + types)显示角标;至此 8 类列表全有角标。②动态列表滑到上限无法翻页根因=运行态硬截断 MAX_RETAINED_LIST_ITEMS 后守卫永久短路(非接口限频);改为 src/lib/listRetention.ts appendWithRetention:超上限从头部释放(有界滑动窗口)+ index.tsx 锚点补偿 scrollTop 防跳,缓存水合时重新上界。③内存:运行态 240→160,drawerCache 预算再收紧(LRU-3→2,单条目 120→80,全局 300→160,弹幕80→60/评论60→40)且全局预算改按嵌套列表计(recommend recommendList/hotList 旧实现漏计),弹幕/评论运行态加独立上限(400/120)。验证:tsc+build+cargo check+79 tests 全绿(新增 tests/round21.test.mjs 15 例)。

## 任务（规格：/tmp/bilifm_minifix_prompt.md）
1. 小窗模式 Windows 顶部空白 → 已修：titleBar.tsx 加 `app-title-bar-actions` 类，mini 模式恢复容器 opacity（根因：`> *` 淡出沿树相乘压掉 #wds-handle-group 的 opacity:1）
2. Windows WebView 内存优化 → 已修：globals.css 移除 record-disc 常驻 will-change、弹幕/评论文本 will-change+translateZ、scroll-area will-change:scroll-position
3. 拖动栏触发空缺全平台 → 已修：新增全宽 `.app-title-bar-drag-rail`（44px、hover 唤出后接管指针、height 与右上关闭按钮错开）

## 状态
- 已提交 b4fb466 并 push origin+gitee 双远端（tip 校验一致，2026-09-11）；用户无法本地测试，授权直接 push

## 派发历史
- tokenai :free 三连败：GUI 兔子洞 74min / 外部 SIGTERM / 429 额度耗尽（09-17 重置）
- mmz 首两轮死于 400 tool_call_sequence_broken → 根因 models.json mmz.api=openai-responses，改 openai-completions 后一轮通过
- 轮29 内存治理第三轮（A/B/C）：A=Windows WebView2 additionalBrowserArgs——前提核查确认窗口创建点在 Rust（tauri.conf app.windows 为空），故写在 lib.rs 的 WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS；关键坑：wry 默认三项(--disable-features=msWebOOUI,msPdfOOUI,msSmartScreenProtection) 在自定义时失效，必须自带，本轮显式带上并合并 MixedContentAutoupgrade；仅加 `--js-flags=--scavenger_max_new_space_capacity_mb=8`（压 V8 新生代→minor GC 更频繁、堆峰值更低）；磁盘缓存类参数评估后不加（HTTP 缓存是还原 src 秒回的关键）。B=滚动窗口位图主动卸载：新增 src/hooks/useViewportImageUnload.ts（双 IntersectionObserver：远界 2 屏/rootMargin 200% 卸载，近界 1 屏/100% 还原，卸载防抖 200ms、还原立即；root=最近可滚动祖先即抽屉 body；1×1 透明 GIF 占位），ListCard 经 HeroUI Card 的 ref 接入，src 在占位/原图间切换，结构与 DOM 钩子不变；contain-intrinsic-size 撑滚动条不跳。C=只测不改：B站 width=240(graftingImage) 实测返回 240×135 WEBP（240w.webp HTTP200 ~4.2KB，orig 1920×1080 JPEG 165KB），见报告。验证：tsc/build/163 tests(+13) 全绿，cargo check 通过（Windows 目标因宿主无 MSVC lib.exe 无法链接，属工具链缺失非代码问题）；Windows 实机效果待用户验证。
- 轮31 内存治理第四轮（用户实测 2.0.24 后仍反馈有压力；逐项评估用户四方向）：
  ① 列表与 DOM —— content-visibility 轮25 已落地；虚拟滚动**评估后不采纳**：grid 响应式列宽（grid-cols-2 sm:grid-cols-3）需 ResizeObserver 重算行高，真实滚动容器是各抽屉 body（HeroUI Drawer 自带 transform + portal），高度依赖 ResizeObserver 测量，重复测量会与 contain-intrinsic-size 打架、易抖动；且卡片本就是轻量 DOM（含 content-visibility 跳过屏外渲染），真正的驻留大头是 WebView2 基线 + 解码位图，虚拟化的边际收益不抵复杂度与回归风险（已在测试 round31 锁定「未引入 useVirtualizer」）。
  ② 图片与内存 —— (a) 离屏位图卸载（r29）已覆盖 8 个 grid 列表；本轮**补齐网页里最后一块长列表缩略图区域=播放列表行**：新增 PlaylistRow 组件，缩略图接入 useViewportImageUnload（滚出 2 屏换 1px 占位、1 屏内还原），DOM/类名/拖拽语义不变。(b) usePreloadImages 由「一次性全部置 src」改为**有界并发**（PRELOAD_CONCURRENCY=2，完成一张补一张），压低首次滚动/切歌瞬间的解码位图叠加；预热顺序=列表头部=首屏可见区，天然优先可见区域。(c) 封面 URL width=240 冻结不动（未提交 DPI 欠采样改动）。
  ③ Tauri/WebView2 —— 窗口失焦 / 关闭到托盘时把 WebView2 内存目标等级降到 LOW（要求回收可释放内存 + 后台节流），重新聚焦恢复 NORMAL；用 ICoreWebView2_19::SetMemoryUsageTargetLevel（with_webview 下发），旧 Runtime 取不到接口时静默降级。新增 Windows-only 依赖 webview2-com/windows-core（Cargo target 段）。「保持 WebView2 Runtime 更新」属外部运行时，代码侧无法强制。
  ④ 排查与验收 —— 审计事件监听（add/remove 成对，无残留）、定时器（player 的 4 个 setTimeout/interval 均有 clear）、全局缓存（sponsorBlock LRU-64、drawerCache 单条 64/全局预算 128，均有界），未发现无界累积/Detached DOM 来源；在测试 round31 固化「有界」契约。验收：tsc/build/195 tests(+15) 全绿，host cargo check 通过；Windows 目标 cargo check 因宿主缺 mingw/msvc C 工具链在 ring 编 C 阶段即失败（工具链缺失，非本轮代码）；已用等版本依赖在 windows 目标单独 type-check 验证 set_webview_memory_usage_target 全链路类型正确。
- 轮32 用户三条新建议（①Windows 任务栏悬浮小窗媒体控制 ②记忆迷你模式窗口位置 ③标题/简介可选中复制）：
  ③ 标题/简介可复制（用户最终裁决：**不要独立复制按钮，可选就行**）：根因是 html 全局 `user-select: none`（且 main.tsx 全局 preventDefault 掉 contextmenu，右键里也没有「复制」），而 `#video-title, #video-desc` 这条 id 规则又把它写死了一次。修法只有一处：把这条 id 规则从 `none` 改成 `user-select: text` + `cursor: text`（id 特异度 1,0,0 压得住 `.selectable-text` 的 0,1,0，加 class 没用）。中途做过的「复制简介按钮 → 复制下拉（标题/简介/标题+简介）」已按用户要求整体撤掉（含 src/utils/clipboard.ts），videoInfo.tsx 回到与改动前逐字节一致；html 上的全局 none 保留，其余 UI 仍是原生 App 观感。已知取舍：标题 2 行 / 简介 3 行有 -webkit-line-clamp，选中只能拿到可见部分（用户接受）。复制入口 = 选中 + Ctrl/Cmd+C（`cursor: text` 是唯一可发现性提示）。
  ② 迷你窗口位置：新增 Rust `set_window_position(x,y)`，多屏语义三条——(1) 目标点仍在某块屏的 **work_area** 内 → **原样恢复**（保留用户刻意悬挂在屏幕边缘/跨两块屏的姿势，不自作主张挪动；左上角在可用区内 ⇒ 标题栏一定抓得到）；(2) 目标点不在任何 work_area 内（副屏被拔、分辨率变小、停在任务栏条或屏幕间隙）→ 夹到**最近**那块屏（点到 work_area 钳位距离平方最小）并保证整个窗口在屏内；(3) 夹取用的物理尺寸按**目标屏缩放**换算（当前屏 outer_size → /当前屏缩放 → 逻辑 → ×目标屏缩放），否则混合 DPI 双屏（100% 主屏 + 150% 副屏）下会溢出 (target−current)×400px —— 这条是自审时用仿真跑出来的真缺陷（旧写法在 150% 副屏右下角算出 (4080,1245)，落地 600×233 直接溢出屏幕 200px）。用 work_area 而不是屏幕 bounds 判定，是为了把「停在任务栏那一条上」归入需要救援的情况。JS 的 Monitor 不暴露 workArea，所以必须在 Rust 做。前端 localStorage 存 `miniWindowPosition`；恢复只在进迷你模式那条分支（resize 之后）；落盘走 `getCurrentWindow().onMoved()` + 300ms 防抖，两道守卫防写脏：`windowModeChangingRef.current` 跳过 + 尺寸守卫（innerWidth>420 / innerHeight>180 即非迷你窗，出模式时的「先 resize 再居中」天然被排除）。迷你尺寸/守卫阈值同源常量（MINI_WINDOW_WIDTH/HEIGHT）。
  ① Windows 任务栏缩略图工具栏（悬停任务栏图标预览浮层里的上一首/播放暂停/下一首）：现状=WebView2 只含 Blink 的 mediaSession，不含 Chrome 浏览器层对 SMTC 的集成，Windows 上看不到任何系统级媒体控件（已有 global-shortcut 只是键盘兜底）。新增 src-tauri/src/taskbar.rs（非 Windows 整体 no-op）：CoCreateInstance(CLSID_TaskbarList)→HrInit→ThumbBarAddButtons 3 按钮→SetWindowSubclass 接管 WM_COMMAND/THBN_CLICKED→emit("taskbar-media", prev|playpause|next")，其余消息 DefSubclassProc 透传给 wry；前端 listen 后复用 mediaNavigationRef/setIsPlaying，并按 isPlaying/canNavigateNext 调 set_taskbar_media_state 换 play↔pause 图标与禁用态。图标不落盘：几何形状 4×4 超采样光栅化成 32bpp DIB + CreateIconIndirect，按 GetDpiForWindow 缩放（16/24/32px），按系统主题（注册表 AppsUseLightTheme）取白/近黑色，主题切换时重建。Cargo 仅 Windows 目标加 windows 0.61（tauri/webview2-com 同版本，已在 lock 里，只多开 feature，无新依赖树）。
  验收：tsc/build 通过，217 tests 全绿（新增 tests/text-selection.test.mjs 4 例、tests/mini-window-position.test.mjs 9 例、tests/taskbar-media.test.mjs 9 例）；host cargo check 通过。顺手修了两处**既有**的测试脆弱点：tests/mini-mode-regression.test.mjs 与 tests/round18-windows-fixes.test.mjs 用 `readdirSync(dist).find(/^index-.*\.js$/)` 取 bundle —— 入口现在被 rollup 拆成「主 chunk + 99 字节 re-export 壳」，readdirSync 顺序跟文件名哈希漂移，会随机取到壳文件导致 4 个 dist 断言假失败（HEAD 上同样会中招，不是本轮引入）；改为按文件名排序后拼接所有 index-*.js。多屏夹取逻辑另用独立 Rust 仿真（/tmp/poscheck，复刻决策逻辑 + 假显示器列表）跑了 18 个布局：单屏/左右副屏/负坐标/混合 DPI/副屏拔掉/副屏变小/屏幕比窗口还小/任务栏条，断言「左上角一定可见 + 救援路径一定完整落在屏内 + 用户放的位置原样恢复」全通过。①的 Windows 专属代码在宿主上无法编译（cfg(windows) 被裁掉，且整包 windows 目标 cargo check 会死在 ring 需要 MSVC），改用 /tmp 脚手架（最小 tauri 替身 + 从 taskbar.rs 原样抽出的 imp 模块）在 x86_64-pc-windows-msvc 目标 `cargo check` 通过（0 error / 0 warning）；另把字形函数抽出来单独跑了一遍 16×16 覆盖率渲染确认四个图标几何正确（prev=|◀ / play=▶ / pause=‖ / next=▶|，覆盖率 14.6%~18.2%）。**①的真实效果（悬停是否出现按钮、DPI 下图标清晰度、点击是否生效）必须 Windows 实机验证**；已知 v1 限制：explorer.exe 重启后缩略图工具栏需重加（未监听 TaskbarCreated），从托盘恢复后按钮是否仍在也待实机确认。
