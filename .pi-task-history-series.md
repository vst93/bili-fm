# 优化：历史记录 tab 持久化 + 合集页面高度修复

## 项目信息
- 根目录：`/home/tar/workspace/bili-fm`
- 技术栈：Tauri v2 + React + TypeScript

## 需求 1：历史记录 tab 切换状态持久化

**现状**：`src/components/historyList.tsx` 第 60 行：
```tsx
const [activeTab, setActiveTab] = useState<HistoryTab>("history");
```
每次打开历史记录抽屉，tab 都重置为"观看历史"。

**目标**：记住用户上次选择的 tab，下次打开时恢复。

**改动**：
1. `useState` 初始化时从 `localStorage.getItem("historyActiveTab")` 读取，fallback 到 `"history"`
2. `setActiveTab` 时同步写入 `localStorage.setItem("historyActiveTab", tab)`
3. 类型安全：读取时校验值是否为 `"history"` 或 `"watchlater"`

## 需求 2：合集页面高度问题

**现状**：`src/components/seriesList.tsx` 第 118-121 行：
```tsx
classNames={{
  base: "h-[92vh] max-h-[calc(100vh-54px)]",
}}
```

`src/styles/globals.css` 第 2734 行：
```css
max-height: min(92vh, calc(100vh - 54px)) !important;
```

**问题分析**：
- `seriesList.tsx` 的 Drawer 使用 `h-[92vh]` 固定高度
- 但 CSS 中 `[data-slot="wrapper"] > section` 的 `max-height: min(92vh, calc(100vh - 54px)) !important;` 可能覆盖 Drawer 自身的高度设置
- 导致合集 Drawer 高度被错误约束（可能过高或过低，取决于视口）

**目标**：
- 合集 Drawer 高度应该自适应内容，而不是固定 `92vh`
- 或者如果内容少时应该紧凑，内容多时滚动
- 与历史记录 Drawer 的表现一致（历史记录用的是同样的 `h-[92vh] max-h-[calc(100vh-54px)]`，但用户没说历史记录有问题，说明问题可能出在别的地方）

**请 pi 仔细排查**：
1. 对比 `historyList.tsx` 和 `seriesList.tsx` 的 Drawer 配置差异
2. 检查 `globals.css` 中 `[data-slot="wrapper"] > section` 规则是否对两者有不同影响
3. 检查 `seriesList.tsx` 的 `DrawerBody` 是否有正确设置 `overflow-y: auto`
4. 检查 `seriesVideos` 为空时是否高度塌陷
5. 给出修复方案并实现

## 约束
1. **只改**：
   - `src/components/historyList.tsx`（tab 持久化）
   - `src/components/seriesList.tsx`（高度修复）
   - `src/styles/globals.css`（如果需要 CSS 修复）
2. 不要改 `player.tsx` / `playerVideo.tsx`
3. 不要引入新 npm 依赖
4. 不要 commit/push
5. 完成后 `npm run build` 必须通过

## 验证
1. `npm run build` 通过
2. `git diff --stat` 只改了上述 2-3 个文件
3. 历史记录：选择"稍后再看" → 关闭抽屉 → 重新打开 → 默认在"稍后再看" tab
4. 合集：打开不同合集的 Drawer，高度表现正常
