# bili-FM Tauri v2 全面重构任务规格

## 项目概述

将 bili-FM 从 Wails v2 (Go + React) 重构为 Tauri v2 (Rust + React)。
原项目代码在 `main.go` `app.go` `menu.go` `tray_*.go` `service/*.go` `frontend/src/**`。
新项目代码在 `src-tauri/src/**` (Rust) + 复用现有 `frontend/src/**` (React)。

## 关键约束

### 1. 登录态无缝迁移（最重要）
旧版(Wails)用自定义 dkv 存储。dkv 是基于文件的 KV 存储：
- 路径: `os.UserConfigDir()/bili-fm/data.db/` 目录
- 每个 key 存为 `<md5(key)[4:12]>.json` 文件，内容为 JSON 数组 `[key, value]`
- md5("SESSDATA")[4:12] 的结果需计算确认

新版(Tauri)必须能读取旧版 dkv 存储的数据，确保老用户升级后登录态不丢失。
**实现方案**: 在 Rust 中实现 dkv 兼容读取层，用相同路径 `config_dir/bili-fm/data.db/`，相同 md5 hash，相同 JSON `[key, value]` 格式。写入也用相同格式，保证向后兼容。
key 包括: `SESSDATA`(登录cookie), `uname`, `face`, `mid`, `playlist`, `playlist_play_mode`

### 2. 前端框架
- React 19 + TypeScript + Vite (已有，保留 `frontend/` 目录)
- UI 库: HeroUI (@heroui/react) — 保留不变
- 图标: @icon-park/react — 保留不变
- 播放器: react-audio-play (音频), react-player (视频) — 保留不变
- CSS: globals.css — 保留并优化

### 3. 前端调用变更
所有 Wails binding 调用需替换为 Tauri invoke：
```typescript
// 旧: import { SearchVideo } from "../../wailsjs/go/service/BL"
// 新: import { invoke } from "@tauri-apps/api/core"; invoke("search_video", { keyword, order })
```
所有 Wails runtime 调用需替换：
```typescript
// 旧: window.runtime.Hide() / BrowserOpenURL()
// 新: import { getCurrentWindow } from "@tauri-apps/api/window"; getCurrentWindow().hide()
// 新: import { openUrl } from "@tauri-apps/plugin-shell"; openUrl(url)
```

### 4. 性能优化点
#### a. 首页碟片旋转
旧版: `animation: record-spin 22s linear infinite` 持续旋转，CSS animation 始终运行。
优化: 播放时用 CSS animation，暂停时用 `animation-play-state: paused`。
但更深层问题是: 即使用 paused，animation 仍会触发合成层。优化方案:
- 旋转使用 `will-change: transform` 提升到合成层
- 暂停时移除 animation 改用 static transform
- 使用 `transform: translateZ(0)` 强制 GPU 加速
- 考虑用 `requestAnimationFrame` + JS 控制，在非可见区域暂停

#### b. 选集列表卡片相邻光效
旧版: App.tsx 中 pointermove 事件遍历所有卡片计算距离，设置 CSS 变量。
问题: radius=180px，每次 pointermove 都遍历全部卡片(getBoundingClientRect)，大量 calc()/color-mix()。
优化:
- 已有 rAF 节流(单 listener + requestAnimationFrame)
- 优化 radius 从 180 降到 120
- 已有缓存机制 cachedCards
- 进一步: 使用 IntersectionObserver 仅处理可见卡片
- 去掉 color-mix()，用预计算的 rgba 值

### 5. Bug 顺手修复
- `GetLoginQRCodeStatus()` 中 `time.Sleep` 阻塞前端(改为异步)
- `GetSESSDATA()` 有双重 GetItem 调用(性能浪费)
- 版本比较用字符串 `>` 比较不可靠(如 "1.9.5" > "1.10.0" 为 true)
- `ioutil.ReadAll` 已废弃，用 `io.ReadAll`

## 架构设计

### Rust 后端结构
```
src-tauri/
├── Cargo.toml
├── build.rs
├── tauri.conf.json
├── capabilities/
│   └── default.json
├── icons/              # 复用现有 frontend/public/logo.png 等转换
└── src/
    ├── main.rs         # 入口, 调用 lib::run()
    ├── lib.rs          # app builder, 插件注册, 命令注册, 窗口/托盘设置
    ├── dkv.rs          # dkv 兼容存储(读取旧版数据)
    ├── bilibili.rs     # 所有 B站 API 调用
    ├── proxy.rs        # 图片代理 HTTP server
    ├── tray.rs         # 系统托盘(跨平台)
    └── commands.rs     # #[tauri::command] 桥接函数
```

### Rust 依赖 (Cargo.toml)
```toml
[package]
name = "bili-fm"
version = "2.0.0"
edition = "2021"

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-shell = "2"
tauri-plugin-store = "2"
tauri-plugin-global-shortcut = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", features = ["json", "rustls-tls"], default-features = false }
tokio = { version = "1", features = ["full"] }
md5 = "0.7"
base64 = "0.22"
flate2 = "1"          # gzip 解压弹幕
html-escape = "0.2"   # 搜索结果去 HTML 标签
image = "0.25"         # 图标处理
```

### Tauri 命令映射 (Wails -> Tauri)

| Wails 方法 | Tauri 命令名 | 参数 |
|------------|-------------|------|
| SearchVideo | search_video | keyword: String, order: String |
| GetCList | get_clist | bvid: String |
| GetUrlByCid | get_url_by_cid | aid: i64, cid: i64 |
| GetLoginQRCode | get_login_qrcode | |
| GetLoginStatus | get_login_status | |
| GetLoginQRCodeStatus | get_login_qrcode_status | |
| SetLoginStatus | set_login_status | status: bool |
| GetSESSDATA | get_sessdata | |
| SetSESSDATA | set_sessdata | data: String |
| GetBLUserInfo | get_user_info | |
| GetBLFeedList | get_feed_list | offset: String |
| GetBLRCMDList | get_rcmd_list | page: i32 |
| GetBLFavFolderList | get_fav_folder_list | |
| GetBLFavFolderListDetail | get_fav_folder_detail | fid: i32, page: i32 |
| GetUpVideoList | get_up_video_list | host_mid: i32, offset: String |
| GetBLHistoryList | get_history_list | max: i32, view_at: i32, business: String, ps: i32 |
| GetSeriesList | get_series_list | mid: i32 |
| GetSeriesVideos | get_series_videos | mid: i32, series_id: i32, page_num: i32 |
| GetBLPopularList | get_popular_list | page: i32 |
| GetDanmakuList | get_danmaku_list | cid: i64 |
| GetReplyList | get_reply_list | oid: i64, page: i32 |
| LikeVideo | like_video | bid: String, like: i32 |
| HasLiked | has_liked | bid: String |
| CoinVideo | coin_video | bid: String, multiply: i32 |
| HasCoin | has_coin | bid: String |
| Follow | follow | mid: i32 |
| Unfollow | unfollow | mid: i32 |
| IsFollowing | is_following | mid: i32 |
| ReportPlayProgress | report_play_progress | aid: i32, cid: i32, progress: i32 |
| ProxyImage | proxy_image_url | url: String |
| FetchImage | fetch_image | url: String |
| GetImageProxyPort | get_image_proxy_port | |
| GetPlaylist | get_playlist | |
| SetPlaylist | set_playlist | playlist_json: String |
| GetPlaylistPlayMode | get_playlist_play_mode | |
| SetPlaylistPlayMode | set_playlist_play_mode | mode: String |
| CheckForUpdates | check_for_updates | is_manual: bool, git_from: String |
| GetPlatform | get_platform | |
| CloseApp | quit_app | |
| GetAppVersion | get_app_version | |

### 窗口配置 (tauri.conf.json)
```json
{
  "app": {
    "windows": [{
      "title": "bili-FM",
      "width": 800,
      "height": 600,
      "resizable": false,
      "fullscreen": false,
      "decorations": false,
      "transparent": false,
      "center": true
    }],
    "security": {
      "csp": "default-src 'self'; img-src 'self' data: asset: http://asset.localhost http://127.0.0.1:4654; style-src 'self' 'unsafe-inline'; media-src 'self' data: blob: http://127.0.0.1:4654 https://*.bilivideo.com; script-src 'self'"
    }
  }
}
```

### 托盘
跨平台用 Tauri 内建 `TrayIconBuilder`：
- macOS: 原生菜单栏，点关闭隐藏窗口，CMD+Q 退出
- Windows: 系统托盘图标，右键菜单"显示窗口"/"退出"
- Linux: 系统托盘图标(用 `TrayIconBuilder`)，右键菜单

### 图片代理
保留内嵌 HTTP 图片代理服务器(Rust 实现)，监听 127.0.0.1:4654。
功能: 转发 B站 CDN 图片请求，附加 Referer 和 User-Agent 头。
同时保留 `fetch_image` Tauri 命令作为 base64 直传备用。

### 迷你模式
窗口缩小为迷你播放器。用 Tauri 的 `set_size` 实现。
注意: Linux 下 webkit2gtk frameless 无法运行时调窗口大小，需平台判断。

### 版本比较修复
Rust 中实现正确的 semver 比较，不用字符串比较。

## 发版工作流 (参考 floter)
- workflow_dispatch 触发
- 自动从 tauri.conf.json 计算版本 PATCH+1
- 6 平台矩阵构建: macOS AS/Intel, Linux x86_64/ARM64, Windows x86_64/ARM64
- 产物命名: bili-FM-{version}-{platform}.{ext}
- tag 无 v 前缀(保持 bili-FM 原有惯例)
- GitHub Release 自动创建 + 产物上传
- 不需要 updater(旧版没有，新版也不需要)
